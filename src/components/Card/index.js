import styled from 'styled-components';

const Card = styled.div`
  ${tw`flex flex-col bg-white m-2 rounded-def`};
  box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2),
    0 6px 20px 0 rgba(0, 0, 0, 0.19);
`;

export default Card;
